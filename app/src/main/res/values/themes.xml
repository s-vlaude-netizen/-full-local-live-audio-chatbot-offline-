<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Compose faerbt die Oberflaeche selbst; das Plattform-Theme liefert nur den
      Rahmen. "DayNight" gibt es hier nicht als Plattform-Theme - der
      Dunkelmodus kommt aus values-night.
    -->
    <style name="Theme.LocalLiveChat" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
